# CAN Interface Settings
can:
  interface: slcan
  bitrate: 500000
  serial_port: null

# Capture Settings
capture:
  mode: continuous
  duration: null
  count: null
  no_console: false
  show_parsed: true

# Output Settings
output:
  directory: data
  formats: []

# DBC Parsing
dbc:
  file: null
  filter: null